							<%= simple_form_for [@commentable, @comment], remote: true do |f| %>
								<%= f.hidden_field :parent_id, :value => local_assigns[:parent_com_id] %>

							    <div class="row">
							      <div class="large-9 columns">
							        <%= f.text_area :body, rows: 4, :placeholder => "" %>

							      </div>
							      <div class="large-3 columns">
							      	<%= f.button :submit, 'Reply It', :class => "tiny " %>
							      </div>
							    </div>
							<% end %>
<% page_title "BEAUTYMEETS > #{@category.try(:name)}" %>
<% page_desc "Discovery, Learn, and Be beautiful in BEAUTYMEETS!! " %>

<%= @category.try(:name) %>

<%= link_to 'Edit', edit_category_path(@category)  if can? :manage, @category %>

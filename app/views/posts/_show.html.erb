<% page_title "BEAUTYMEETS > #{post.try(:title)} " %>
<% page_thumb "#{full_url(post.thumbnail.image_url)}" if post.thumbnail.present? %>
<% page_desc "Discovery, Learn, and Be beautiful in BEAUTYMEETS!!" %>

<div class="text-right"><%= link_to "Edit", edit_post_path(post) if user_signed_in && current_user.can_uaduate?(post) %></div>
<div class="row">
	<div class="small-12 medium-4 columns">
		<%= image_tag post.thumbnail.image_url(:large) if post.thumbnail.present? %>
	</div>
	<div class="small-12 medium-8 columns">
      <h2 class="subheader"><%= best_in_place post, :title %></h2>
      <h3><small class="right"><%= render 'bookmarks/star' %></small></h3>
      <h6 class="subheader ">
				<% if post.categories.present? %>
					<small>Category : </small>
					<% post.categories.each do |category| %>
						&nbsp;<span class=" secondary tiny label"><%= link_to category.name, category_path(category) %></span>
					<% end %>
				<% end %>
			</h6>
      
      
			<p><%= post.description %></p>
    	<p>
    		<% post.tag_list.each do |t| %>
	  	  	<span class="label secondary radius"><%= link_to t, tag_path(t) %></span>
		   	<% end %>
			</p>
  </div>
</div>

<% @pictures = post.pictures if post.pictures %>

<div class="row" style="">
	<div class="small-12 columns " >
		<%= render partial: 'pictures/gallery', locals: { thumb_size: :medium } %>
	</div>
</div>


		<div style="margin:10px 0;">
    	<div class="fb-like" data-href="<%= post_url(post) %>" data-layout="button_count" data-action="like" data-show-faces="true" data-share="true"></div>
    </div>
<%
	if (@commentable.class.name == "Item")
  	mailer_title = @commentable.try(:name)
  else
  	mailer_title = @commentable.try(:title)
	end
%>
<%= mailer_title %>에 작성한 댓글에 <%= @comment.user.name || @comment.user.email %>님의 답변이 달렸습니다.안녕하세요, 뷰티밋츠입니다. 
			뷰티밋츠에 게제된 작성물, <%= mailer_title %>에 작성하셨던 댓글에 <%= @comment.user.name || @comment.user.email %>님의 답변이 달렸어요.
		
			내가 쓴글 : <%= simple_format(@parent.try(:body)) %>
			질문자 : <%= @comment.user.name || @comment.user.email %>
			질문내용 : <%= simple_format(@comment.try(:body)) %>
		
			확인해주시고 필요하면 답변을 부탁드려요.
			감사하다고 말씀해주시면 더욱 좋겠죠?
		
		
 			궁금하신 사항은 언제든지 아래 메일주소로 연락주세요.<br/>
 			감사합니다.
 		-  뷰티밋츠 팀(hellobeauty@reallplay.com) -
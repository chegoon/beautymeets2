
  <div class="small-12 medium-6 large-4  columns" style="text-align:center; margin-bottom:24px;">
    <div class="tutorial-card"  style="">
      <div class="row ">
        <div class="small-12 columns" style="text-align:center; padding:5px;">
            <a class="" href="<%= tutorial_path(tutorial) %>" style='
              -moz-box-shadow: inset 0 0 100px -50px rgba(0, 0, 0, 0.7);
              -webkit-box-shadow: inset 0 0 100px -50px rgba(0, 0, 0, 0.7);
              box-shadow: inset 0 0 100px -50px rgba(0, 0, 0, 0.7);
              margin:0 auto;
              background-repeat: no-repeat; 
              background-size: 100%; 
              background-position: center; 
              max-width:280px; width:100%; 
              height:170px;

              display:block; background-image: url("<%= tutorial.thumbnail.image_url(:medium) if tutorial.thumbnail.present? %>");'>

            <% if tutorial.categories.present? %>
              <% tutorial.categories.each do |category| %>
                &nbsp;<span class=" secondary  label right"><%= category.name %></span>
              <% end %> 
            <% end %>
            </a>
        </div>
      </div>
      <div class="row " >
        <div class="small-12 columns " >          
          <div style="position: relative; margin:0 auto; min-height:80px; max-width:300px;">
            <h6 class="tutorial_title subheader text-left" style="line-height:120%; margin:0 10px; "><a href="<%= tutorial_path(tutorial) %>" ><%= truncate(tutorial.try(:title), 44) %></a>&nbsp;<span class="right" style="color:#ff8008;"><%= "New" if (Time.zone.now - tutorial.created_at).to_i / 1.day <= 3 %></span>
            </h6>
            <h6 class="subheader  text-left" style="font-size: 0.8em; margin:5px 10px;  color:#999; ">
              <%= render partial: 'users/avatar_thumb', locals: { user: tutorial.author } if tutorial.author.present? && tutorial.author.image.present? %>
              &nbsp;<span class="group_name" ><strong><%=  link_to tutorial.author.profile.try(:fullname), beautystar_path(tutorial.author.profile) %></strong>&nbsp;<%= tutorial.author.profile.try(:job_title) %></span>

              
            </h6>    
            <h5 class="subheader " style="position: absolute; bottom: 0; right:0; margin:0 10px;  color:#999; "><small><i class="fa fa-eye"></i> <%=  tutorial.view_count.nil? ? "0" : number_with_delimiter(tutorial.view_count) %></small>
            </h5>
          </div>
        </div>
      </div>
    </div>
  </div>

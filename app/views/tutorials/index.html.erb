<%= link_to 'New', new_tutorial_path if (can? :create, Tutorial) %>
<% page_title "BEAUTYMEETS > #{@tutorials.first.title}" %>
<% page_thumb "#{full_url(@tutorials.first.try(:thumbnail).image_url)}" if @tutorials.first.thumbnail.present? %>
<% page_desc "Discover, Learn, and Be beautiful in BEAUTYMEETS!! #{@tutorials.first.title} 영상을 만나보세요." %>


			<div class="row">
				<div class="small-12 small-centered columns" style="padding:0;">

					<div id="tutorials">		

						<!-- <div data-magellan-expedition="fixed" style="background: #f5f5f5; padding:0; margin:0;"> -->
						<div style="background: #f5f5f5; padding:0; margin:0;">
							<dl class="button-group radius" style="padding-top:10px;  width:200px; margin:0 auto; ">
						  <dd><a class="button small <%= 'secondary' if params[:order].nil? || params[:order] == 'newly' %> sort"  data-sort="view_count" href=<%= "#{tutorials_path}?order=popular" %>>Popular</a></dd>
						  <dd><a class="button small <%= 'secondary' if params[:order].nil? || params[:order] == 'popular' %> sort"  data-sort="published_at" href=<%= "#{tutorials_path}?order=newly" %>>Newly</a></dd>
							</dl>
						</div>
						<div class="list">	  
							<%= render @tutorials %>
  					</div>						
					</div>
			  </div>
			</div>

			<div class="pagination-centered"><%= will_paginate @tutorials, previous_label: "&laquo;".html_safe, next_label: "&raquo;".html_safe , renderer: FoundationPagination::Rails %></div>

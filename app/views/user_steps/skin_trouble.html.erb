<div class="progress small-12 success radius" style="border:0;">
  <span class="meter" style="width: 95%"></span>
</div>
<h3>피부고민을 체크해주세요.</h3>
<h5>여러개를 선택하셔도 됩니다. </h5>
<%= simple_form_for @member, url: wizard_path do |f| %>
	<%= f.input :skin_trouble_list, :as => :check_boxes, :collection => Member.tag_counts_on("skin_troubles").map(&:name), :hint => "Please check all that apply"%>
	<%= f.button :submit, "Continue" %>
	<%= link_to "Skip", next_wizard_path, :class => "button secondary" %>
<% end %>
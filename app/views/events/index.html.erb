<h3 class="subheader text-center" style="margin:30px;">EVENTS</h3>


<%= render partial: @events, locals: { gate_tag: true }  %>
<br />

<%= link_to 'New Event', new_event_path if (can? :manage, Event) %>


  <div class="small-6 medium-3 large-3 columns" style="text-align:center">
    <div class="video-card"  >
      <div class="row ">
        <div class="small-12  " style="text-align:center; padding:5px;">
          <div>
            <a class="video-card-thumb " href="<%= video_path(video) %>" style='
              -moz-box-shadow: inset 0 0 100px -50px rgba(0, 0, 0, 0.7);
              -webkit-box-shadow: inset 0 0 100px -50px rgba(0, 0, 0, 0.7);
              box-shadow: inset 0 0 100px -50px rgba(0, 0, 0, 0.7);
              margin:0 auto;
              background-repeat: no-repeat; 
              background-size: 100%; 
              background-position: center; 
              max-width:280px; width:100%; 
              height:120px;

              display:block;background-image: url(<%= "#{video.thumb_url}" %>);'>
              <i class="fa fa-play-circle fa-3x" style="position:absolute; color:#FFF; top:45px; margin:0 auto;"></i>
              <span style="background-color:rgba(51, 51, 51, 0.35); color:#EEE; padding:2px 3px; font-size:0.8rem; position:absolute; bottom:10px; right:26px;"><%= Time.at(video.duration).utc.strftime("%-M:%S") if video.duration.present? %></span>
            </a>
          </div>
        </div>
      </div>
      <div class="row " >
        <div class="small-12 " style="text-align:left; padding:0 5px;">          
          <div style="position: relative; min-height:70px;">
          <h6 class="video_title subheader " style="line-height:120%; margin:0 10px; "><a href="<%= video_path(video) %>" ><%= truncate(video.try(:title), 44) %></a>&nbsp;<span class="right" style="color:#ff8008;"><%= "New" if (Time.zone.now - video.created_at).to_i / 1.day <= 3 %></span>
          </h6>
          <h6 class="subheader" style="font-size: 0.8em; margin:5px 10px;  color:#999; ">
            <% if video.video_group.present? && video.video_group.image.present? %>
              <%= image_tag video.video_group.image_url(:thumb).to_s, :style => "position: absolute; top: 40px;width:24px; height:24px;" %>
            <% else %>
              <%= image_tag video.video_group.try(:thumb_url), :style => "position: absolute; top: 40px;width:24px; height:24px;" %>
            <% end %>
            &nbsp;<span class="group_name" style="position: absolute; top: 44px; left:40px; "><%=  link_to video.video_group.try(:name), video_group_path(video.video_group) %></span>
          </h6>          
          <h6 class="subheader " style="position: absolute; bottom: 0; right:0; font-size: 0.8em; margin:0 10px;  color:#999; "><i class="fa fa-eye"></i> <%=  video.view_count.nil? ? "0" : number_with_delimiter(video.view_count) %></h6>
          </div>
        </div>
      </div>

    </div>
  </div>
